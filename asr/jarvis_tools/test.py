from color_recognition.data import ColorData

# roi_list = [[1, 2, 3, 4], [5, 6, 7, 8]]
# color_dict = {'1' : ColorData(),
#               '2' : ColorData(),}

# for roi, color_name, color_values in zip(roi_list, color_dict.keys(), color_dict.values()):
#     print(roi, color_name, color_values.hsv_min, color_values.hsv_max)

test_set = set([2, 3, 4, 1])

print(test_set)